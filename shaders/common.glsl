const float GAMMA_CORRECTION = 2.2;

const float KA = 0.1;
const float SUN_PATH_ROTATION = -40.0;

const int shadowMapResolution = 8192;
const float SHADOW_STRENGTH = 0.5; // At 0.0 shadows don't exist, at 1.0 shadow is darkest